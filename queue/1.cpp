#include <iostream>
#include <queue>

using namespace std;

int main()
{
    // fifo-first in frist out
    // push pop size empty
    // queue.front();
    //
    queue<int> s;
    deque<int> ss;
    ss.
    s.push(2);
    s.push(3);
    s.push(4);
    s.push(5);
    s.
    s.swap()

    cout << "peak element is- " << s.size();

    // reverse;
}